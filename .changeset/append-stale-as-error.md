---
---

Chore: `Entity.append()` refactor (stale-as-error + `.skipFollowUp()`, closes #33). Version bump intentionally deferred to a coordinated release PR — see PR description for the migration narrative that should land in the CHANGELOG when this is eventually versioned.
