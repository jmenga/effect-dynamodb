{
  "name": "@effect-dynamodb/doctest",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:connected": "vitest run --config vitest.connected.ts",
    "check": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "effect": "4.0.0-beta.48",
    "typescript": "^5.7.0",
    "vitest": "^4.1.0"
  }
}
